- check the asset for direction to only include two strings
X check for duplicated colors
X delete the old map
X remove the ft_strlen for everyloop
X init ray directions (is facing up/down/right/left)
- refactor all raycasting functions calls
- check if all the attributes are initialized
- intersection not working properly in the joint of two walls
X add parsing rule for doors

- fix slideways movements 

--save
        "/Users/osallak/Desktop/normed-version-cub3d/assets/door1.xpm"

- fix mouse move (keeps rotating)