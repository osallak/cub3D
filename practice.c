#include<stdio.h>
#include<stdlib.h>
#include<math.h>
#include<mlx.h>
#include <signal.h>

#define HIGHT 1920
#define WIDTH 1080


int main()
{
    // void *mlx = mlx_init();
    // void *win = mlx_new_window(mlx, WIDTH, HIGHT, "practice");

    // void *img = mlx_new_image(mlx, 100, 100);

    printf("%d\n", 7263.24 % 323);
}